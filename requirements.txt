# Библиотеки, нужные движку Jardin Rose.
# Установка: python -m pip install -r requirements.txt

# Веб-сервер: маршруты, сессии, отдача страниц и JSON
Flask>=3.1

# Работа с изображениями: генерация графики, обрезка фотографий
Pillow>=11.0

# Доверие к сертификатам HTTPS из хранилища Windows.
# Нужна только при скачивании фотографий и шрифтов: без неё Python
# не может качать через антивирус или корпоративный прокси.
truststore>=0.10

# База данных sqlite3 входит в состав Python — устанавливать не нужно.
